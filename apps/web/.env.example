# ── GraphQL API ───────────────────────────────────────────────
# Server-side URL (used during SSR / build)
GRAPHQL_URL=http://localhost:3000/graphql

# Client-side URL (exposed to the browser)
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:3000/graphql

# ── App ───────────────────────────────────────────────────────
NODE_ENV=development
